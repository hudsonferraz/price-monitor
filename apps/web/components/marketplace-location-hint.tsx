export function MarketplaceLocationHint() {
  return (
    <p className="rounded-md border border-[var(--border)] bg-[var(--background)] px-4 py-3 text-sm text-[var(--muted)]">
      Results follow your Facebook account&apos;s region — listings are shown near where your
      Facebook session is logged in, not a city you pick in the app.
    </p>
  );
}
