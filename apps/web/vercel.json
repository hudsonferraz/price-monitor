{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && npm run build --workspace=@price-monitor/web",
  "crons": [
    {
      "path": "/api/cron/wake-worker",
      "schedule": "*/5 * * * *"
    }
  ]
}
